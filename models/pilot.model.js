class Pilot {
  constructor(pilotRegistration, pilotName, pilotAge, pilotTypeRating) {
    this._id = null;
    (this.pilotRegistration = pilotRegistration),
      (this.pilotName = pilotName),
      (this.pilotAge = pilotAge),
      (this.pilotTypeRating = pilotTypeRating);
  }
}
